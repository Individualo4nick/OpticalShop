apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: optical-db-data
  name: optical-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
