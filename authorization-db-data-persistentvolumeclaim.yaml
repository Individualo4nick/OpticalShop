apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: authorization-db-data
  name: authorization-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
